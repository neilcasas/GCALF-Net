# @package __global__
defaults:
  - gcalf_grade_pilot

# Stage 3 Fix B. This historical configuration is retained so the original
# confounded pilot remains reproducible; use gcalf_grade_remediation_bprime
# for the corrected diagnostic.
augment_cfg:
  dataloader_kwargs:
    grade_balanced_sampling: true
