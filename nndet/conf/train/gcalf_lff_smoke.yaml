# @package __global__
defaults:
  - gcalf_smoke

model_cfg:
  encoder_kwargs:
    gcalf_cfg:
      frequency_filter_type: lff
      lff:
        radius: 0.15
        grid_size: [4, 8, 8]
        groups: 1
